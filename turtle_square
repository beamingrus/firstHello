import turtle
mt=turtle.Turtle()
x=100
y=90
def square(x,y):
  mt.forward(x)
  mt.left(y)
  mt.forward(x)
  mt.left(y)
  mt.forward(x)
  mt.left(y)
  mt.forward(x)
  mt.left(y+15)
  
for c in range(0,36):
  square(x,y)
